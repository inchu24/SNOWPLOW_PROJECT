name: "{project_name}"
version: "1.0.0"
config-version: 2
profile: "{project_name}"
model-paths:
  - models
analysis-paths:
  - analysis
test-paths:
  - tests
data-paths:
  - data
macro-paths:
  - macros
target-path: target
clean-targets:
  - target
  - dbt_modules
models:
  {project_name}:
    snowplow_unified:
      enabled: true
      {vars_block}
